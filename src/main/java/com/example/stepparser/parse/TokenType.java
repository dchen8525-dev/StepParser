package com.example.stepparser.parse;

enum TokenType {
    KEYWORD,
    IDENTIFIER,
    STRING,
    INTEGER,
    REAL,
    ENUM,
    INSTANCE_NAME,
    EQUALS,
    COMMA,
    SEMICOLON,
    LEFT_PAREN,
    RIGHT_PAREN,
    DOLLAR,
    STAR,
    END_OF_INPUT
}
